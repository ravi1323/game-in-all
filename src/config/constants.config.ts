export const CONSTANTS = {
    SOCKET: {
        EVENTS: {
            CORE: {
                DISCONNECT: 'disconnect',
                CONNECT: 'connection'
            },
            CUSTOM: {
                TEST: 'TEST'
            }
        }
    },
}